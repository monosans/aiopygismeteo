version: 2
build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"
  commands:
    - asdf plugin add uv
    - asdf install uv latest
    - asdf global uv latest
    - uv run --frozen --no-cache mkdocs build -d $READTHEDOCS_OUTPUT/html
